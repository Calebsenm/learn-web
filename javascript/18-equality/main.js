
// = assigment operator 
// == comparison operator (compare if values values are equal)
// === strict equality operator (compare if values and datatype are equal)
// != inequality operator (compare if values & datatype are equal)
// !== strict inequality operator 

const PI = 3.14 

if (PI == "3.14"){
    console.log('that is PI'); 
}   else {
    console.log('that is not PI');     
}
